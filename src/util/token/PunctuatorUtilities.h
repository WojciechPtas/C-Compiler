#pragma once

#include <string>
#include "../../model/token/Punctuator.h"

namespace c4 {
    namespace util {
        namespace token {
            const std::string &stringify(model::token::Punctuator punctuator);
        }
    }
}
