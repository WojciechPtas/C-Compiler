#pragma once

#include <string>
#include "../../model/token/Keyword.h"

namespace c4 {
    namespace util {
        namespace token {
            const std::string &stringify(model::token::Keyword keyword);
        }
    }
}
